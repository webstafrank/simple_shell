#include "shell.h"

/**
 * main - Entry point to test building a linked list of PATH directories
 *
 * Description: This program demonstrates building a linked list of
 * directories from the PATH environment variable and printing them.
 *
 * Return: Always 0 on success
 */

int main(void)
	{
	/* Build linked list of PATH directories and print them */
	build_path_list();
	return (0);
}
